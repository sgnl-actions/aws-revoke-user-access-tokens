# Generated by sgnl-test-init — fill in the TODOs below
#
# Scenarios with "record: true" will be recorded by: npx sgnl-test-record
# Fill in real secrets in action.context.secrets, then run the recorder.
# After recording, redact secrets before committing.
action:
  params:
    userName: test-user-1
    region: us-east-1
  context:
    secrets:
      BASIC_USERNAME: username
      BASIC_PASSWORD: password
    environment: {}
scenarios:
  - name: Base Success Case
    steps:
      - request:
          method: POST
          url: https://iam.amazonaws.com/
        fixture: fixtures/base-success-case-step1.http
      - request:
          method: POST
          url: https://iam.amazonaws.com/
        fixture: fixtures/base-success-case-step2.http
    invoke:
      returns:
        userName: test-user-1
        keysDeleted: 1
        revoked: true
  - name: Access Keys Alreay Revoked
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/access-keys-alreay-revoked.http
    invoke:
      returns:
        userName: test-user-1
        keysDeleted: 0
        revoked: true
  - name: User Does Not Exist
    params:
      userName: test-user-2
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/user-does-not-exist.http
    invoke:
      throws: "User not found: test-user-2"
  - name: Invalid Credentials
    context:
      secrets:
        BASIC_USERNAME: invalid-username
        BASIC_PASSWORD: invalid-password
    request:
      method: POST
      url: https://iam.amazonaws.com/
    fixture: fixtures/invalid-credentials.http
    invoke:
      throws: "Failed to list access keys: The security token included in the request is invalid."
